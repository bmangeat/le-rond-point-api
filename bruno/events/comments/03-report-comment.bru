meta {
  name: Report Comment
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/groups/{{groupId}}/events/{{eventId}}/comments/{{commentId}}/report
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "reason": "Contenu inapproprié"
  }
}
