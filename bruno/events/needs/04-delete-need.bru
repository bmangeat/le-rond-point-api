meta {
  name: Delete Need
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/groups/{{groupId}}/events/{{eventId}}/needs/{{needId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
