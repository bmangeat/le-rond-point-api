meta {
  name: Update Member Role
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/groups/{{groupId}}/members/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "role": "ADMIN"
  }
}

docs {
  role: "ADMIN" | "MEMBER"
  Réservé aux admins du groupe.
}
