meta {
  name: Create Group
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/groups
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Le Rond Point de Belleville"
  }
}

docs {
  Copier l'id retourné dans la variable d'environnement `groupId`.
}
