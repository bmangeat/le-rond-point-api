meta {
  name: Delete Invitation
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/groups/{{groupId}}/admin/invitations/{{invitationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
