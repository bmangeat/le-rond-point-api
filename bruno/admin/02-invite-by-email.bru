meta {
  name: Invite by Email
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/groups/{{groupId}}/admin/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "ami@example.com"
  }
}
